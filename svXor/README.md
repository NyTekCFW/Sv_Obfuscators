# svXor
a C++ Compile-time string obfuscation (C++11 +)

## Disclaimer
This obfuscator requires the flags -Os and -s to prevent the compiler
from reintroducing clear string literals during optimization.
